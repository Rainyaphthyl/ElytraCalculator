package org.naftalluvia.algorithm;

/**
 * Given a certain trail of operations by ticks, returns a trail of player movement.
 */
public class Predictor {
    public Predictor(ATrailOperation trailOperation) {
        System.out.printf("Operation Trail (%s) in Predictor (%s)\n", trailOperation, this);
    }
}
